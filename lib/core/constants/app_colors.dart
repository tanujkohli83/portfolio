import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color scaffoldBg = Color(0xFF020617);
  static const Color cardBg = Color(0xFF0F172A);
  static const Color editorBg = Color(0xFF1E293B);

  // Accents
  static const Color primary = Color(0xFF3B82F6); // Electric Blue
  static const Color secondary = Color(0xFF6366F1); // Indigo
  static const Color accent = Color(0xFF10B981); // Emerald (for Kotlin val/etc)

  // Text
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textDim = Color(0xFF64748B);

  // Terminal/Code Colors
  static const Color codeKeyword = Color(0xFFF472B6);
  static const Color codeString = Color(0xFF34D399);
  static const Color codeComment = Color(0xFF64748B);
  static const Color codeNumber = Color(0xFFFBBF24);

  // Borders
  static const Color border = Color(0xFF1E293B);
}
